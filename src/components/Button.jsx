import React from 'react'

const Button = ({title}) => {
  return (
    <a href="#" className="inline-block  my-10 p-4 text-white btn-grad w-[150px] text-center ">{title}</a>
  )
}

export default Button